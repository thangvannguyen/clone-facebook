import React from 'react';
import PropTypes from 'prop-types';

Noti.propTypes = {

};

function Noti(props) {
    return (

        <span className="header-noti"> {props.number} </span>

    );
}

export default Noti;